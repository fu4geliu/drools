# DRL 落盘目录（存规则时编译并写入此处）
neurosymbolic:
  drl:
    output-dir: target/generated-rules

spring:
  application:
    name: Neuro-Symbolic
  ai:
    openai:
      base-url: ？
      api-key: ？
      chat:
        options:
          model: ？